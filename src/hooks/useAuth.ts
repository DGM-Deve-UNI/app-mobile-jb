// import { useContext } from "react";
// // Importe o AuthContext (que está no Canvas)
// import { AuthContext } from "@/src/contexts/AuthContext";
// // Importe o tipo, se necessário (ele deve estar em src/types/auth.ts ou dentro do AuthContext)
// // import { AuthContextType } from "@/src/types/auth";

// // Re-exporta o hook do Contexto
// export const useAuth = () => {
//   const context = useContext(AuthContext);
//   if (context === undefined) {
//     throw new Error("useAuth must be used within an AuthProvider");
//   }
//   // O tipo de 'context' será inferido automaticamente pelo AuthContext.Provider
//   return context;
// };
